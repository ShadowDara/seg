[build-system]
requires = [ "setuptools>=61", "wheel" ]
build-backend = "setuptools.build_meta"

[project]
name = "seg"
version = "0.2.7"
requires-python = ">=3.8"

[project.scripts]
seg = "seg.cli:main"

[tool.setuptools.packages.find]
include = [ "seg*" ]
